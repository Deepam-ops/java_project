# java_project
this is a java project to be tested for jenkins

HelloWorld
==========

A simple Java application that can be compiled into a .jar file using Maven. This is a update after the webhook is applied.

To build
--------
    mvn clean package

To run
------
    java -cp target/helloworld-1.1.jar com.coveros.demo.helloworld.HelloWorld
